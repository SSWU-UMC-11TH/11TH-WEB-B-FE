export default function Header() {
    return (
        <header>
            <strong>UMCine</strong>

            <nav>
                <span>영화</span>
                <span>검색</span>
                <span>내 정보</span>
            </nav>

            <button type="button">로그인</button>
        </header>
    );
}